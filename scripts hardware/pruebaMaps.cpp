#include <map>
#include <string>

void setup() {
    std::map<std::string, std::string> ; 

    persona["comando"] = ;
    persona["pin"] = ;
    persona["max"] = ;
    persona["min"] = ;
    persona["flex"] = ;
    persona["ext"] = ;
    return 0;
}